<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <p class="btn btn-primary"><%= f.button :submit%></p>
<% end %>

<%= link_to "Go Back to your List", list_path(@list.id) %>
<%= link_to "Go Back to Lists", lists_path %>
